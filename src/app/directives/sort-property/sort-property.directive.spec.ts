import { SortPropertyDirective } from './sort-property.directive';

describe('SortPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new SortPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
